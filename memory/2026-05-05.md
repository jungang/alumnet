# 2026-05-05 开发日志

## 阶段一进展：安全底线 + 可用性修复

### 已完成

- [x] **1.1 requireAdmin 权限修复** — 允许 `super_admin` 角色，修正 401/403 语义
- [x] **1.2 authMiddleware 静默降级修复** — 管理/认证路径无效 Token 返回 401，公开 API 允许 Guest 降级
- [x] **1.3 RAG 安全加固** — 输入长度限制 500 字符、14 种注入模式检测、输入清洗
- [x] **1.4 安全基建** — Helmet 安全头 + CORS origin 限制 + 数据库 SSL 修复 + 移除敏感配置日志
- [x] **1.6 ILIKE 通配符注入修复** — `%` `_` 转义，防止全表扫描
- [x] **1.15 RAG System Prompt 可配置化** — 从环境变量 + system_config 表读取学校信息，管理后台可修改 AI 人设
- [x] **Graceful shutdown** — SIGTERM/SIGINT 信号处理 + 依赖清理
- [x] **RAG 超时控制** — 所有 fetch 调用加 AbortController 超时
- [x] **authService 修复** — roleHierarchy 加入 super_admin、adminLogin 读取实际角色、检查 status=active、记录 last_login_at
- [x] **auth 单元测试更新** — 21 个测试全部通过（含新的 super_admin 测试）

### 新增 API 端点
- `GET /api/admin/rag-config` — 获取 RAG/AI 配置
- `PUT /api/admin/rag-config` — 更新 RAG/AI 配置（含 school_name, school_since, school_motto, rag_system_prompt）
- `POST /api/admin/rag-cache/clear` — 清除 RAG 缓存

### 新增环境变量
- `CORS_ORIGINS` — 逗号分隔允许的前端域名
- `RAG_MAX_INPUT_LENGTH` — RAG 查询最大输入长度（默认 500）
- `RAG_QUERY_TIMEOUT_MS` — RAG 单次查询超时（默认 30000ms）
- `SCHOOL_MOTTO` — 校训（可配置）
- `DB_SSL_REJECT_UNAUTHORIZED` — SSL 证书验证控制
- `DB_SSL_CA_PATH` — 自定义 CA 证书路径

### 新增数据库配置项
- `school_name` — 学校名称
- `school_since` — 创建年份
- `school_motto` — 校训
- `rag_system_prompt` — 自定义 AI 人设（支持变量替换）

### 执行阶段补充记录

**代码修改文件清单**：
- `server/src/middleware/auth.ts` — 完全重写，修复 requireAdmin + authMiddleware 静默降级
- `server/src/services/authService.ts` — roleHierarchy 加 super_admin、adminLogin 读实际角色
- `server/src/services/ragService.ts` — 完全重写：输入清洗、注入检测(14种)、ILIKE转义、System Prompt 可配置化、超时控制、缓存刷新
- `server/src/app.ts` — 加 Helmet、CORS 限制、graceful shutdown、CORS 错误处理
- `server/src/config/database.ts` — SSL 修复、移除敏感日志
- `server/src/routes/api-docs.ts` — 类型修复
- `server/src/routes/admin.ts` — 新增 3 个 RAG 配置端点
- `server/src/db/init.sql` — 新增 4 个系统配置项
- `server/src/types/models.ts` — UserRole 加入 super_admin
- `server/.env.example` — 新增 CORS_ORIGINS、RAG 安全配置、学校配置
- `server/tests/unit/middleware/auth.test.ts` — 完全重写，21 个测试全通过
- `server/package.json` — 新增 helmet 依赖
- `docs/DEVELOPMENT_PLAN.md` — v2.0 修订版，综合五位评审意见

**编译和测试状态**：
- TypeScript: `tsc --noEmit` 零错误
- 全量测试: 280 passed / 2 failed (预先存在的 filters.test.ts)

**用户确立的核心原则**：
- 只增不减 — 不舍弃任何现有功能或设计
- 不要负面优化 — 不为简化工期砍功能
- 拉长工期确保全面完整 — 工期可延长，质量不打折
- 尽可能增加、丰富、增强 — 每项功能只做加法

### 测试报告总结（02:17 执行）

**全项目测试状态**：
- Server: 280 passed / 2 failed (filters.test.ts — department 字段未实现，预先存在)
- Client: 149 passed / 4 failed (theme.property.test.ts — 预先存在)
- Admin: 52 passed / 2 failed (api/index.test.ts — alert 在 Node 环境不存在，预先存在)
- 合计: 481 passed / 8 failed → 98.2% 通过率
- **所有 8 个失败均为预先存在的问题，与安全修复无关**

**覆盖率核心问题**：
- vitest.config.ts 排除了 9 个模块（routes, repositories, ragService 等），人为抬高覆盖率
- 真实覆盖率估算 ~15-20%（含被排除模块）
- 零集成测试、零 E2E 测试
- v8 coverage provider 未正确生成最终报告（只有 .tmp 原始文件）

**用户新增要求**：
- 遵循 spec 驱动开发（先写规格，再实现）
- 端到端测试 + 单元测试完整覆盖
- "只增不减"原则 + spec 驱动开发 = 两个核心方法论

### 待完成
- [x] 1.5 uploads 目录认证 — ✅ 已完成（app.ts referer/origin 检查中间件）
- [x] 1.7-1.11 触屏可用性修复（前端）— ✅ 已完成（见下方详细记录）
- [x] 1.12-1.14 集成测试框架 + RAG 注入测试 — ✅ 已完成（114 个集成测试全通过）

---

## 05:00 执行记录 — 三路并行完成

### 1.5 uploads 目录认证 ✅
- `app.ts` 中已有 referer/origin 检查中间件，在 `express.static(uploadPath)` 之前
- 无 referer 的直接访问被拒绝

### 1.7-1.11 前端触屏可用性修复 ✅

**1.7 触控目标最小 48px**：
- `style.css` — 竖屏模式 `.touch-target` 强制 `min-width/min-height: 48px`
- `BottomNavigation.vue` — nav-item 已有 touch-target 类
- `AIChatDialog.vue` — 快捷提问按钮、发送按钮全部加 touch-target
- `GalaxyView.vue` — 翻页按钮、分类按钮、动态导航全部加 touch-target

**1.8 字体最小 12px**：
- 清除所有 `text-[8px]` — 0 处残留
- 清除所有 `text-[10px]` — 0 处残留
- 替换为 `text-xs`（12px）或 `text-sm`（14px）
- 涉及文件：BottomNavigation、AIChatDialog、SearchView、GalaxyView、HomeView、InteractionView、AlumniDetailView、ClassRosterWallView、ServiceView、TimeCorridorView、VintageMuseumView、PageHeader

**1.9 AIChatDialog 接入主题系统** ✅：
- 引入 `useThemeStore` + `isDark` computed
- 对话框容器：深色 `bg-[#0a0f1a]` ↔ 浅色 `bg-[#faf8f5]`
- 头部渐变、AI头像、消息气泡、输入框、快捷提问 — 全部响应主题切换
- 关闭按钮、清空按钮 — 主题适配

**1.10 GalaxyView 接入主题系统** ✅：
- 引入 `useThemeStore` + `isDark` computed
- 主容器：深色 `bg-[#020608]` ↔ 浅色 `bg-[#faf8f5]`
- 背景装饰光效 — 主题适配
- 头部导航、标题渐变 — 主题适配
- 分类Tab 激活/未激活状态 — 双主题配色
- 翻页按钮 — 深色黑底 ↔ 浅色白底
- 校友卡片照片区、信息区 — 主题适配
- 新闻走马灯 — 主题适配
- 加载/空状态 — 主题适配

**1.11 色彩对比度修复** ✅：
- `text-white/30` → `text-white/50` 或 `text-[#2d1810]/50`（WCAG AA ≥4.5:1）
- `text-gray-400/30` → 更高透明度
- `text-teal-200/40` → `text-teal-200/50`
- 空状态文字对比度统一提升
- 低对比度问题最严重的 AIChatDialog、GalaxyView 已完全重构

### 1.12-1.14 集成测试框架 ✅

**1.12 集成测试框架建立**：
- 安装 `supertest` + `@types/supertest`
- `tests/integration/setup.ts` — 模拟数据库池、JWT 辅助函数、mock 工具
- `tests/integration/helpers.ts` — 测试客户端工厂
- `app.ts` — 测试环境跳过 `app.listen()`
- `api.integration.test.ts` — 23 个端点测试，覆盖：
  - 健康检查、校友搜索(4)、校友详情(2)、筛选选项
  - 杰出校友、班级名录、毕业照、老物件、留言、捐赠项目
  - RAG 查询(3)、认证、管理员统计(2)、uploads 认证、响应格式(2)

**1.13 requireAdmin 权限集成测试**：
- `admin-auth.integration.test.ts` — 11 个测试
- 验证矩阵：未认证/空头/格式错误 → 401/403
- 伪造Token/错误密钥/过期Token → 401/403
- user/guest → 403
- admin/super_admin → 200 ✅（验证 P0-2 修复）
- 401/403 语义区分

**1.14 RAG 注入测试用例库**：
- `rag-injection.integration.test.ts` — 80 个测试
- 60 个注入变体（7 类：direct/roleplay/leak/exfiltration/sql/i18n/combo）
- 15 个合法查询（零误判）
- 长度限制测试
- 控制字符清理测试
- 属性测试：`it.each` 驱动

**集成测试结果**：114/114 全部通过
**全项目测试结果**：394/396 通过（2个预先存在的 filters.test.ts 失败）
**前端构建**：TypeScript 类型检查 ✅ / Vite build ✅

---

## 20:50 执行记录 — Phase 2 基建完成

### 2.1 admin.ts 拆分 ✅
- 17 个子路由文件 + index.ts，每文件 ≤200 行
- 原文件 `admin.ts` 已删除，备份也已删除
- 所有子路由使用 `asyncHandler()` + `AppError` 错误体系
- 共享 `logOperation` → `utils/logOperation.ts`
- tsc 零错误，vitest 394/396 通过

### 2.2 ESLint flat config ✅
- server + client 均迁移到 `eslint.config.mjs`
- 旧 `.eslintrc.cjs` 已删除
- lint 脚本更新：`eslint src`（不再需要 `--ext`）
- server: 0 errors, ~277 warnings (no-console + no-explicit-any)
- client: 0 errors, ~50 warnings
- 安装 `eslint-plugin-vue` + `vue-eslint-parser`

### 2.3 zod 输入验证层 ✅
- `validation/schemas.ts` — 20+ schema 定义
- `validate()` 中间件 — 自动解析 + 校验 + 400 响应
- 所有写操作 API 已 wire: alumni, messages, distinguished, photos, notices, donations, associations, news, vintage, classRosters, videoGreetings, knowledge, rag-config, config, rag-query

### 2.4 统一错误处理 ✅ (Phase 1 已完成)
- AppError + 8 个子类 + asyncHandler + globalErrorHandler
- pino 日志集成（运营错误 warn，非运营 error + 堆栈）

### 2.5 数据库迁移 (node-pg-migrate) ✅
- `dbconfig.js` 配置文件
- `migrations/001_system_config_and_logs.js` — 初始迁移
- npm scripts: `db:migrate`, `db:rollback`, `db:create-migration`

### 2.6 pino 结构化日志 ✅
- `config/logger.ts` — dev 用 pino-pretty，prod 用 JSON
- app.ts / database.ts / errorHandler.ts / ragService.ts — 全部迁移到 logger
- 环境变量: `LOG_LEVEL` (默认 dev=debug, prod=info)

### 2.7 Graceful shutdown 增强 ✅
- 关闭状态标志 `isShuttingDown`
- 新连接拒绝（socket.destroy）
- 健康检查关闭期间返回 503
- 现有请求排空（最多 8 秒等待）
- uncaughtException / unhandledRejection 捕获 → 触发关闭

### 2.8 CI/CD ✅
- `.github/workflows/ci.yml` 重写
- 三阶段: lint → test (type-check + vitest) → build
- 并发取消、覆盖率上传、构建产物上传
- pnpm v10 + Node 20

### 2.9 RAG 熔断器 ✅
- `CircuitBreaker` 类: closed → open (5次失败) → half-open (60秒后试探) → closed (成功)
- 集成到 `RAGService.query()`: 熔断时自动降级到关键词检索
- 环境变量: `RAG_CB_FAILURE_THRESHOLD`, `RAG_CB_RESET_TIMEOUT_MS`, `RAG_CB_HALF_OPEN_ATTEMPTS`
- 响应增加 `degraded` 标记

### 2.12 3D 星图原型 ✅
- `Galaxy3D.vue` 组件 — Three.js + OrbitControls
- 校友为发光星点，按行业着色（8 种行业色）
- 年份映射到径向距离（越近越新），行业映射到角度扇区
- 鼠标悬停 → tooltip 显示校友信息
- 点击星点 → 跳转详情页
- 自动旋转、双指缩放、拖拽旋转
- GalaxyView 新增 ✦ 切换按钮：卡片 ↔ 3D 星图
- 主题适配（深色/浅色背景 + 雾效）

### 多角色评审记录
- 产品经理：需修改（前5周无用户感知 → 每阶段并行用户可感知交付）
- 技术架构师：需修改（安全遗漏 6 项 → 已修复 5 项）
- UI/UX 设计师：需修改（主题/触屏/对比度 → 待执行）
- 测试工程师：需修改（0 集成测试 → 待建框架）
- 外部评估：需修改（时间偏乐观 → 20→24 周）
