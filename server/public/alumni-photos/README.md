# Alumni Photos / 校友照片

Place alumni portrait photos in this directory.
在此目录放置校友头像照片。

Naming convention: `{name}.jpg` or `{name}.png`
命名规范：`{姓名}.jpg` 或 `{姓名}.png`

These photos will be used in the exhibition display.
这些照片将用于展厅展示。